# Gemfile/Gemfile.lock are NOW committed (Cloudflare Pages needs them to build).
# Remove the ignore lines that previously excluded them.

# Jekyll build output (generated, never commit)
_site/

# Ignored Folders
**/__pycache__/
__pycache__/
